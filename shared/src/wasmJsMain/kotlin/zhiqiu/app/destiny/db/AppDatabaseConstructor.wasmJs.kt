// AppDatabaseConstructor 的 actual 由 Room 3 通过 @ConstructedBy + expect object 自动生成，
// 不要手写 actual。运行时请勿直接调用 AppDatabaseConstructor.initialize()，
// 应通过 getRoomDatabase(getDatabaseBuilder()) 取得数据库实例。
